services:
  backend:
    container_name: vrl-backend
    ports:
      - "5000:5000"
    env_file:
      - .env.backend

  frontend:
    container_name: vrl-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.frontend
    depends_on:
      - backend
