services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    container_name: vrl-backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
    volumes:
      - ./Backend:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - internal
    command: npm start

  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    container_name: vrl-frontend
    ports:
      - "3000:3000"
    env_file:
      - ./Frontend/.env
    volumes:
      - ./Frontend:/app
      - /app/node_modules
    networks:
      - internal
    command: npm run dev
    depends_on:
      - backend
